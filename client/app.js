$('form').on('submit',function () {
  var text = $('#message').val();
  alert(text);
  return false;
});

/* event-driven programming: asking to be notified when a given event takes place and waiting for it to happen*/
